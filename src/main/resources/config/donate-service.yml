server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://35.240.239.126:3306/donate_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: app_user
    password: AppUser@2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://10.10.0.3:8761/eureka/
  instance:
    prefer-ip-address: true